FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir fastapi uvicorn

# Copy simple backend
COPY simple_backend.py .

EXPOSE 7860

CMD ["uvicorn", "simple_backend:app", "--host", "0.0.0.0", "--port", "7860"]
